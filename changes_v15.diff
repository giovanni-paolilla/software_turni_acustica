Only in /tmp/turni_v14_base: __pycache__
Only in /tmp/turni_v14_base: changes_v12.diff
Only in /tmp/turni_v14_base: changes_v13.diff
Only in /tmp/turni_v14_base: changes_v14.diff
Only in /mnt/data/turni_v15_release: changes_v15.diff
diff -ru /tmp/turni_v14_base/requirements.txt /mnt/data/turni_v15_release/requirements.txt
--- /tmp/turni_v14_base/requirements.txt	2026-04-07 15:05:35.000000000 +0000
+++ /mnt/data/turni_v15_release/requirements.txt	2026-04-07 17:49:53.452618490 +0000
@@ -1 +1 @@
-ortools==9.9.*
+ortools==9.9.3963
Only in /tmp/turni_v14_base/tests: __pycache__
diff -ru /tmp/turni_v14_base/tests/test_helpers.py /mnt/data/turni_v15_release/tests/test_helpers.py
--- /tmp/turni_v14_base/tests/test_helpers.py	2026-04-07 15:06:34.000000000 +0000
+++ /mnt/data/turni_v15_release/tests/test_helpers.py	2026-04-07 17:49:53.439384449 +0000
@@ -1,3 +1,4 @@
+import json
 import importlib.util
 import os
 import time
@@ -7,40 +8,40 @@
 import unittest
 from unittest import mock
 
-MODULE_PATH = pathlib.Path(__file__).resolve().parents[1] / "turni_v14.py"
-spec = importlib.util.spec_from_file_location("turni_v14", MODULE_PATH)
-turni_v14 = importlib.util.module_from_spec(spec)
+MODULE_PATH = pathlib.Path(__file__).resolve().parents[1] / "turni_v15.py"
+spec = importlib.util.spec_from_file_location("turni_v15", MODULE_PATH)
+turni_v15 = importlib.util.module_from_spec(spec)
 assert spec.loader is not None
-spec.loader.exec_module(turni_v14)
+spec.loader.exec_module(turni_v15)
 
 
 class HelperTests(unittest.TestCase):
     def test_normalize_name_collapses_spaces_and_case(self):
-        self.assertEqual(turni_v14.normalize_name("  Mario   Rossi  "), "mario rossi")
+        self.assertEqual(turni_v15.normalize_name("  Mario   Rossi  "), "mario rossi")
 
     def test_safe_csv_cell_prefixes_formula_like_values(self):
-        self.assertEqual(turni_v14._safe_csv_cell("=2+2"), "'=2+2")
-        self.assertEqual(turni_v14._safe_csv_cell(" @cmd"), "' @cmd")
-        self.assertEqual(turni_v14._safe_csv_cell("Mario Rossi"), "Mario Rossi")
+        self.assertEqual(turni_v15._safe_csv_cell("=2+2"), "'=2+2")
+        self.assertEqual(turni_v15._safe_csv_cell(" @cmd"), "' @cmd")
+        self.assertEqual(turni_v15._safe_csv_cell("Mario Rossi"), "Mario Rossi")
 
     def test_atomic_write_replaces_existing_file_contents(self):
         with tempfile.TemporaryDirectory() as tmp:
             target = pathlib.Path(tmp) / "sessione.json"
             target.write_text("vecchio", encoding="utf-8")
-            turni_v14._write_text_file_atomic(str(target), '{"ok": true}')
+            turni_v15._write_text_file_atomic(str(target), '{"ok": true}')
             self.assertEqual(target.read_text(encoding="utf-8"), '{"ok": true}')
 
     def test_target_file_lock_blocks_second_writer(self):
         with tempfile.TemporaryDirectory() as tmp:
             target = pathlib.Path(tmp) / "sessione.json"
-            with turni_v14._TargetFileLock(str(target)):
+            with turni_v15._TargetFileLock(str(target)):
                 with self.assertRaises(BlockingIOError):
-                    with turni_v14._TargetFileLock(str(target)):
+                    with turni_v15._TargetFileLock(str(target)):
                         self.fail("il secondo lock non dovrebbe essere acquisibile")
             self.assertFalse((pathlib.Path(str(target) + ".lock")).exists())
 
     def test_dedupe_normalized_texts_preserves_order_and_reports_duplicates(self):
-        unique_values, unique_norms, duplicates = turni_v14._dedupe_normalized_texts(
+        unique_values, unique_norms, duplicates = turni_v15._dedupe_normalized_texts(
             ["Gennaio", " gennaio ", "Febbraio", "FEBBRAIO"]
         )
         self.assertEqual(unique_values, ["Gennaio", "Febbraio"])
@@ -48,7 +49,7 @@
         self.assertEqual(duplicates, [" gennaio ", "FEBBRAIO"])
 
     def test_group_weeks_by_normalized_month_uses_normalized_key(self):
-        grouped = turni_v14._group_weeks_by_normalized_month([
+        grouped = turni_v15._group_weeks_by_normalized_month([
             {"month": "Gennaio", "week": "Sett.1"},
             {"month": " gennaio ", "week": "Sett.2"},
             {"month": "Febbraio", "week": "Sett.3"},
@@ -57,17 +58,17 @@
         self.assertEqual([w["week"] for w in grouped["febbraio"]], ["Sett.3"])
 
     def test_find_blank_text_entries_detects_empty_or_space_only_strings(self):
-        blanks = turni_v14._find_blank_text_entries(["Mario", "   ", "", "Anna"])
+        blanks = turni_v15._find_blank_text_entries(["Mario", "   ", "", "Anna"])
         self.assertEqual(blanks, ["   ", ""])
 
     def test_normalized_week_key_ignores_case_and_extra_spaces(self):
         self.assertEqual(
-            turni_v14._normalized_week_key(" Gennaio ", "  Sett.1  "),
+            turni_v15._normalized_week_key(" Gennaio ", "  Sett.1  "),
             ("gennaio", "sett.1"),
         )
 
     def test_find_duplicate_week_keys_uses_normalized_month_and_week(self):
-        duplicates = turni_v14._find_duplicate_week_keys([
+        duplicates = turni_v15._find_duplicate_week_keys([
             {"month": "Gennaio", "week": "Sett.1"},
             {"month": " gennaio ", "week": " sett.1 "},
             {"month": "Febbraio", "week": "Sett.1"},
@@ -75,7 +76,7 @@
         self.assertEqual(duplicates, [(" gennaio ", " sett.1 ")])
 
     def test_reset_result_state_clears_solver_state(self):
-        solver = turni_v14.TurniSolver(["Mario"], [])
+        solver = turni_v15.TurniSolver(["Mario"], [])
         solver.result_rows = [{"week": "Sett.1"}]
         solver.counts = [3]
         solver.diff_val = 2
@@ -98,9 +99,9 @@
         self.assertFalse(solver.partial_result_available)
         self.assertIsNone(solver.cp_solver)
 
-    @unittest.skipUnless(turni_v14.ORTOOLS_OK, "ortools non installato nel test environment")
+    @unittest.skipUnless(turni_v15.ORTOOLS_OK, "ortools non installato nel test environment")
     def test_solve_marks_cancelled_when_event_is_already_set(self):
-        solver = turni_v14.TurniSolver(["Mario"], [])
+        solver = turni_v15.TurniSolver(["Mario"], [])
         event = threading.Event()
         event.set()
 
@@ -111,11 +112,28 @@
         self.assertFalse(solver.solved_ok)
         self.assertFalse(solver.partial_result_available)
 
+    def test_parse_step0_inputs_requires_at_least_two_operatori(self):
+        with self.assertRaises(turni_v15.SessionValidationError):
+            turni_v15._parse_step0_inputs(
+                "2026",
+                "Gennaio",
+                "Mario",
+                "60",
+            )
+
+    def test_validate_solver_ready_weeks_rejects_less_than_two_available(self):
+        with self.assertRaises(turni_v15.SessionValidationError):
+            turni_v15._validate_solver_ready_weeks(
+                [{"month": "Gennaio", "week": "Sett.1", "busy_indices": [1]}],
+                operator_count=2,
+                error_prefix="Test",
+            )
+
 
 class SessionValidationTests(unittest.TestCase):
     def test_validate_session_payload_rejects_empty_operatori(self):
-        with self.assertRaises(turni_v14.SessionValidationError):
-            turni_v14._validate_session_payload({
+        with self.assertRaises(turni_v15.SessionValidationError):
+            turni_v15._validate_session_payload({
                 "anno": "2026",
                 "operatori": [],
                 "mesi": ["Gennaio"],
@@ -123,8 +141,8 @@
             })
 
     def test_validate_session_payload_rejects_empty_mesi(self):
-        with self.assertRaises(turni_v14.SessionValidationError):
-            turni_v14._validate_session_payload({
+        with self.assertRaises(turni_v15.SessionValidationError):
+            turni_v15._validate_session_payload({
                 "anno": "2026",
                 "operatori": ["Mario"],
                 "mesi": [],
@@ -132,24 +150,24 @@
             })
 
     def test_validate_session_payload_canonicalizes_strings_and_weeks(self):
-        payload = turni_v14._validate_session_payload({
+        payload = turni_v15._validate_session_payload({
             "anno": " 2026 ",
-            "operatori": [" Mario Rossi ", "Anna Verdi"],
+            "operatori": [" Mario Rossi ", "Anna Verdi", "Luca Neri"],
             "mesi": [" Gennaio "],
             "weeks": [{"month": " gennaio ", "week": " Sett.1 ", "busy_indices": [1]}],
             "solver_timeout": " 90 ",
         })
         self.assertEqual(payload["anno"], "2026")
-        self.assertEqual(payload["operatori"], ["Mario Rossi", "Anna Verdi"])
+        self.assertEqual(payload["operatori"], ["Mario Rossi", "Anna Verdi", "Luca Neri"])
         self.assertEqual(payload["mesi"], ["Gennaio"])
         self.assertEqual(payload["weeks"], [{"month": "gennaio", "week": "Sett.1", "busy_indices": [1]}])
         self.assertEqual(payload["solver_timeout"], "90.0")
 
     def test_validate_session_payload_rejects_duplicate_weeks(self):
-        with self.assertRaises(turni_v14.SessionValidationError):
-            turni_v14._validate_session_payload({
+        with self.assertRaises(turni_v15.SessionValidationError):
+            turni_v15._validate_session_payload({
                 "anno": "2026",
-                "operatori": ["Mario"],
+                "operatori": ["Mario", "Anna"],
                 "mesi": ["Gennaio"],
                 "weeks": [
                     {"month": "Gennaio", "week": "Sett.1", "busy_indices": []},
@@ -163,7 +181,7 @@
             lock_path = pathlib.Path(str(target) + ".lock")
             lock_path.write_text('{"pid": 999999, "created_at": 0}', encoding="utf-8")
 
-            with turni_v14._TargetFileLock(str(target)):
+            with turni_v15._TargetFileLock(str(target)):
                 self.assertTrue(lock_path.exists())
 
             self.assertFalse(lock_path.exists())
@@ -172,24 +190,55 @@
         with tempfile.TemporaryDirectory() as tmp:
             lock_path = pathlib.Path(tmp) / "sessione.json.lock"
             lock_path.write_text('not-json', encoding="utf-8")
-            old = time.time() - (turni_v14.LOCK_STALE_SECONDS + 5)
+            old = time.time() - (turni_v15.LOCK_STALE_SECONDS + 5)
             os.utime(lock_path, (old, old))
-            self.assertTrue(turni_v14._is_stale_lock(str(lock_path)))
+            self.assertTrue(turni_v15._is_stale_lock(str(lock_path)))
+
+    def test_validate_session_payload_rejects_single_operatore(self):
+        with self.assertRaises(turni_v15.SessionValidationError):
+            turni_v15._validate_session_payload({
+                "anno": "2026",
+                "operatori": ["Mario"],
+                "mesi": ["Gennaio"],
+                "weeks": [],
+            })
+
+    def test_is_stale_lock_does_not_steal_live_pid_only_for_old_age(self):
+        with tempfile.TemporaryDirectory() as tmp:
+            lock_path = pathlib.Path(tmp) / "sessione.json.lock"
+            lock_path.write_text(json.dumps({
+                "pid": os.getpid(),
+                "created_at": 0,
+                "proc_start_token": turni_v15._get_process_start_token(os.getpid()),
+            }), encoding="utf-8")
+            self.assertFalse(turni_v15._is_stale_lock(str(lock_path), stale_after_seconds=1))
+
+    def test_is_stale_lock_detects_pid_reuse_when_process_token_differs(self):
+        with tempfile.TemporaryDirectory() as tmp:
+            lock_path = pathlib.Path(tmp) / "sessione.json.lock"
+            lock_path.write_text(json.dumps({
+                "pid": os.getpid(),
+                "created_at": time.time(),
+                "proc_start_token": "old-token",
+            }), encoding="utf-8")
+            with mock.patch.object(turni_v15, "_pid_is_alive", return_value=True), \
+                 mock.patch.object(turni_v15, "_get_process_start_token", return_value="new-token"):
+                self.assertTrue(turni_v15._is_stale_lock(str(lock_path)))
 
 
 class RestartAndTimeoutTests(unittest.TestCase):
     def test_validate_session_payload_rejects_invalid_solver_timeout(self):
-        with self.assertRaises(turni_v14.SessionValidationError):
-            turni_v14._validate_session_payload({
+        with self.assertRaises(turni_v15.SessionValidationError):
+            turni_v15._validate_session_payload({
                 "anno": "2026",
-                "operatori": ["Mario"],
+                "operatori": ["Mario", "Anna"],
                 "mesi": ["Gennaio"],
                 "weeks": [],
                 "solver_timeout": "abc",
             })
 
     def test_perform_restart_rebuilds_step1_without_default_rows(self):
-        app = turni_v14.TurniApp.__new__(turni_v14.TurniApp)
+        app = turni_v15.TurniApp.__new__(turni_v15.TurniApp)
         build_calls = []
         shown_steps = []
 
@@ -209,7 +258,7 @@
         app._build_step1_content = lambda add_default_row=True: build_calls.append(add_default_row)
         app._show_step = lambda step: shown_steps.append(step)
 
-        turni_v14.TurniApp._perform_restart(app)
+        turni_v15.TurniApp._perform_restart(app)
 
         self.assertEqual(app.weeks_data, [])
         self.assertIsNone(app._last_solver)
@@ -228,9 +277,9 @@
                     raise OSError("write failure")
                 return real_write(fd, data)
 
-            with mock.patch.object(turni_v14.os, "write", side_effect=flaky_write):
+            with mock.patch.object(turni_v15.os, "write", side_effect=flaky_write):
                 with self.assertRaises(OSError):
-                    with turni_v14._TargetFileLock(str(target)):
+                    with turni_v15._TargetFileLock(str(target)):
                         pass
 
             self.assertFalse(pathlib.Path(str(target) + ".lock").exists())
Only in /tmp/turni_v14_base: turni_v14.py
Only in /mnt/data/turni_v15_release: turni_v15.py
